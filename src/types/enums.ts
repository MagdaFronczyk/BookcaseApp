//api requests statuses
export enum status {
  PENDING = 'PENDING',
  RESOLVED = 'RESOLVED',
  REJECTED = 'REJECTED',
}

export enum panelModalScreenNames {
  SIGN_UP = 'signUp',
  SIGN_IN = 'signIn',
  RESET_PASSWORD = 'resetPassword',
}
