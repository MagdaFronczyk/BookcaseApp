import * as React from 'react';
import {Text} from 'react-native';

const Index: React.FC = (): JSX.Element => {
  return <Text style={{color: 'black'}}>British National Bibliography</Text>;
};

export default Index;
